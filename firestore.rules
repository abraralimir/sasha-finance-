rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For this simplified application, we will allow reads and writes.
    // In a production app, you would lock this down.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
